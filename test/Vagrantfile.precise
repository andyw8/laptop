# -*- mode: ruby -*-
# vi: set ft=ruby :
Vagrant.configure('2') do |config|
  config.vm.box = "precise32"
  config.vm.box_url = "http://files.vagrantup.com/precise32.box"

  # TODO: make our own box with these steps done already
  config.vm.provision :shell, :inline => 'aptitude update'
  config.vm.provision :shell, :inline => 'aptitude install -y base-files'
  config.vm.provision :shell, :inline => 'aptitude install -y ubuntu-desktop'
end
